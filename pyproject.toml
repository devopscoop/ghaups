[project]
name = "ghaups"
version = "0.1.0"
description = "A simple CLI tool that updates GitHub Actions in workflow files to their latest versions."
requires-python = ">=3.11"
dependencies = [
    "requests>=2.33.0",
]
